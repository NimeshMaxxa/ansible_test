---
 - hosts: all
   become: true
   tasks:
 
   - name: copy a file
     tags: site
     copy:
        src: default_site.html
        dest: /var/www/html/index.html
        owner: root
        group: root
        mode: 0644
